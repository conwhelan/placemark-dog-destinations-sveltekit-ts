// Shared subtitle used by pages to update the common heading.
export const subTitle = $state({ text: '' });

// Shared login state.
// TODO extend this with name, token and user id.
export const loggedInUser = $state({ email: '' });
